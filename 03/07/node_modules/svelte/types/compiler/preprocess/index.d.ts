import { PreprocessorGroup, Processed } from 'svelte/types/compiler/preprocess/types';
export * from 'svelte/types/compiler/preprocess/types';
export default function preprocess(source: string, preprocessor: PreprocessorGroup | PreprocessorGroup[], options?: {
    filename?: string;
}): Promise<Processed>;
